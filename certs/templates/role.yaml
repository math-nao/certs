apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: certs
  namespace: app
rules:
- apiGroups: [""]
  resources: ["secrets"]
  verbs: ["get", "list", "update" , "create"]
- apiGroups: ["extensions"]
  resources: ["ingresses"]
  verbs: ["list"]